<UserControl
    x:Class="Colibri.Controls.PlayerMiniControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Height="64">
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <!-- Album art -->
        <Image x:Name="ArtworkImage" Width="48" Height="48" Margin="8" Stretch="UniformToFill" />
        <!-- Transport controls -->
        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="4,0,8,0">
            <Button x:Name="PrevButton" Click="PrevButton_Click" Width="36" Height="36" Margin="0,0,6,0">
                <TextBlock Text="⏮" FontSize="16" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Button>
            <Button x:Name="PlayPauseButton" Click="PlayPauseButton_Click" Width="40" Height="40" Margin="0,0,6,0">
                <TextBlock x:Name="PlayPauseGlyph" Text="▶" FontSize="16" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Button>
            <Button x:Name="NextButton" Click="NextButton_Click" Width="36" Height="36" Margin="0,0,6,0">
                <TextBlock Text="⏭" FontSize="16" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Button>
            <Button x:Name="StopButton" Click="StopButton_Click" Width="36" Height="36">
                <TextBlock Text="■" FontSize="16" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Button>
        </StackPanel>
        <!-- Track info + position -->
        <StackPanel Grid.Column="2" VerticalAlignment="Center" Margin="8,0">
            <TextBlock x:Name="TrackTitle" Text="" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" Margin="0,0,0,2"/>
            <TextBlock x:Name="TrackArtist" Text="" Opacity="0.8" TextTrimming="CharacterEllipsis" Margin="0,0,0,2"/>
            <Slider x:Name="PositionSlider" Minimum="0" Maximum="100" ValueChanged="PositionSlider_ValueChanged"/>
        </StackPanel>
        <TextBlock x:Name="TimeLabel" Grid.Column="3" VerticalAlignment="Center" Margin="8"/>
    </Grid>
</UserControl>
